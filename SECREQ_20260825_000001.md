# OpenAPI definition (orch-service workflow and configuration APIs) - API Security Automation Suite

This markdown file is a container for the generated Postman API Security Automation artifacts for the `OpenAPI definition` / `orch-service` workflows & configuration APIs.

The actual artifacts are committed separately as JSON files:
- `postman_collection.json`
- `postman_environment.json`
- `security_automation_manifest.json`

Each file link is provided below after commit.
