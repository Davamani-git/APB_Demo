(function(){'use strict';angular.module('apbApp').service('credentialService',credentialService);credentialService.$inject=['$q'];function credentialService($q){var store={};this.setCredential=function(provider,cred){store[provider]=cred;};this.getCredential=function(provider){return $q.resolve(store[provider]||null);};this.hasCredential=function(provider){return !!store[provider];};this.validate=function(cred){return !!(cred&&cred.accessKey&&cred.secret);};}})();