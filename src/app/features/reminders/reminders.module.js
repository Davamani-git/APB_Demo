(function () {
  'use strict';

  angular
    .module('rbApp.reminders')
    .config(remindersConfig);

  remindersConfig.$inject = [];

  function remindersConfig() {
    // Placeholder for reminders feature configuration.
  }
})();
