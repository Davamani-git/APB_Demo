// This file can be used to define model factories or constructors if a more formal approach is desired.
// For this project, models are treated as plain JavaScript objects, validated and mapped in the services.

// Example of a model definition (for documentation purposes)
/*
function DashboardSummaryModel(data) {
    this.totalMonthlySpend = data.totalMonthlySpend || 0;
    this.totalCreditLimit = data.totalCreditLimit || 0;
    this.availableCredit = data.availableCredit || 0;
    this.outstandingAmount = data.outstandingAmount || 0;
    this.utilizationPercentage = data.utilizationPercentage || 0;
    this.transactionCount = data.transactionCount || 0;
}
*/